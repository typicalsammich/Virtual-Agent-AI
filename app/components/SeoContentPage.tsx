import type { Metadata } from 'next';
import { SiteHeader } from './SiteChrome';
import { SiteFooter } from './SiteFooter';

export type ContentPageData={path:string;title:string;description:string;eyebrow:string;lead:string;sections:{title:string;body:string}[];bullets?:string[];faqs:{q:string;a:string}[];related:{href:string;label:string}[]};
export function contentMetadata(d:ContentPageData):Metadata{return {title:d.title,description:d.description,alternates:{canonical:d.path},openGraph:{title:d.title,description:d.description,url:d.path,type:'website'}}}
export function SeoContentPage({data}:{data:ContentPageData}){const schema={'@context':'https://schema.org','@graph':[{'@type':'WebPage',name:data.title,description:data.description,url:`https://virtualagentai.com${data.path}`},{'@type':'FAQPage',mainEntity:data.faqs.map(x=>({'@type':'Question',name:x.q,acceptedAnswer:{'@type':'Answer',text:x.a}}))}]};return <><SiteHeader/><main className="seoLanding"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><section className="seoLandingHero"><div><p className="eyebrow">{data.eyebrow}</p><h1>{data.title}</h1><p>{data.lead}</p><div className="seoLandingActions"><a className="button" href="/#contact">Book a tailored demo <span>↗</span></a><a href="/demo">Hear real call examples</a></div></div><aside><small>AI PHONE ANSWERING</small><strong>Answer. Qualify. Book. Transfer.</strong><span>24/7 coverage configured around your business rules.</span></aside></section><section className="seoLandingProcess"><div className="seoLandingProcessIntro"><p className="eyebrow">PRACTICAL DETAILS</p><h2>Built around real business calls.</h2><p>Clear answers for buyers comparing AI receptionist and phone answering options.</p></div><div className="seoLandingSteps">{data.sections.map((s,i)=><article key={s.title}><span>{String(i+1).padStart(2,'0')}</span><h3>{s.title}</h3><p>{s.body}</p></article>)}</div></section>{data.bullets&&<section className="seoLandingFit"><div><p className="eyebrow">WHAT IT CAN HANDLE</p><h2>Common call outcomes</h2></div><ul>{data.bullets.map(b=><li key={b}><span>✓</span>{b}</li>)}</ul></section>}<section className="seoLandingFaq"><div><p className="eyebrow">COMMON QUESTIONS</p><h2>Quick answers before you book.</h2></div><div>{data.faqs.map((f,i)=><details key={f.q} open={i===0}><summary>{f.q}<span>+</span></summary><p>{f.a}</p></details>)}</div></section><section className="seoLandingRelated"><span>KEEP EXPLORING</span><nav>{data.related.map(r=><a key={r.href} href={r.href}>{r.label}<b>↗</b></a>)}</nav></section><section className="seoLandingCta"><p className="eyebrow">SEE IT ON YOUR CALL FLOW</p><h2>Test Virtual Agent AI with a real scenario.</h2><p>Bring the type of call your team receives every day. We will show how the AI receptionist can answer it, capture the right details, and move the caller to the right next step.</p><a className="button" href="/#contact">Book a 15-minute walkthrough <span>↗</span></a></section></main><SiteFooter/></>}
